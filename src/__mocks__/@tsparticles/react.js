import React from 'react';
const Particles = (props) => React.createElement('div', { 'data-testid': 'particles' });
export const initParticlesEngine = () => Promise.resolve();
export default Particles;
